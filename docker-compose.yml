# Build with: docker compose build
# Run with: docker compose up (site at http://localhost:8080)
services:
  jekyll:
    build: .
    image: al-folio-jekyll:latest
    ports:
      - "8080:8080"
    volumes:
      - .:/srv/jekyll
