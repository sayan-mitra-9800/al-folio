_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
vendor
*.sh
# Docker entrypoint scripts (must be tracked for docker build)
!bin/entry_point.sh
!bin/entry_point_nginx.sh

